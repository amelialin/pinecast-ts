export const TOKENS: Array<[RegExp, string | null]> = [
  [/^(?:\r\n|\r|\n)/, null],
  [/^\s+/, null],

  [/^FROM/i, 'from'],
  [/^SELECT/i, 'select'],
  [/^COUNT\s+DISTINCT/i, 'count distinct'],
  [/^COUNT/i, 'count'],
  [/^WHERE/i, 'where'],
  [/^GROUP\s+BY/i, 'group by'],
  [/^AS/i, 'as'],
  [/^BY/i, 'by'],
  [/^BETWEEN/i, 'between'],
  [/^AND/i, 'and'],
  [/^OR/i, 'or'],

  [/^\(/, '('],
  [/^\)/, ')'],

  // [/^IN/i, 'in'],
  // [/^NOT\s+IN/i, 'not in'],
  [/^=/, '='],
  [/^!=/, '!='],

  [/^[a-zA-Z_][\w_]*/, 'identifier'],

  // [/^"(?:\\(?:.|\r\n|\r|\n)|[^"\\\n])*"/i, 'str'],
  // [/^'(?:\\(?:.|\r\n|\r|\n)|[^'\\\n])*'/i, 'str'],
  // [/^#[^\n\r]*/i, 'comment'],
  // // sfloat literals
  // [/^\-?[1-9][0-9]*\.[0-9]+s/, 'sfloat'],
  // [/^\-?0\.[0-9]+s/, 'sfloat'],
  // // Floats must be matched before integers
  // [/^\-?[1-9][0-9]*\.[0-9]+/, 'float'],
  // [/^\-?0\.[0-9]+/, 'float'],
  // [/^\-?[1-9][0-9]*(?!\.)/, 'int'],
  // [/^0(?!\.)/, 'int'],
  // [/^;/, ';'],
  // [/^,/, ','],
  // [/^\./, '.'],
  // [/^\{/, '{'],
  // [/^\}/, '}'],
  // [/^\[:/, '[:'],
  // [/^\[/, '['],
  // [/^\]/, ']'],

  // [/^\+/, '+'],
  // [/^\-/, '-'],
  // [/^\//, '/'],
  // [/^\*/, '*'],
  // [/^%/, '%'],
  // [/^<=/, '<='],
  // [/^>=/, '>='],
  // [/^=/, '='],
  // [/^</, '<'],
  // [/^>/, '>'],
  // [/^:/, ':'], // For types
  // [/^!/, '!'],
  // [/^~/, '~'],
];
