import * as rendering from './rendering';
import {route as routesRoute, routesRoutes} from './routes';

export default rendering;
export const route = routesRoute;
export const routes = routesRotues;
