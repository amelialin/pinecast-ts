import React from 'react';

import {getsContext} from './getsContext';

export default getsContext(({setState, state}) =>
    <div style={{
        flex: '1 1',
        height: '100%',
    }}>
        //
    </div>
);
